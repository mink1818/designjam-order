(function(){
'use strict';
const SENSITIVE_PAGES=new Set(['members.html','customer-settings.html','proxy-order.html','stats.html','settings.html','deleted-orders.html']);
const hiddenPaths=[...SENSITIVE_PAGES];let profile=null;
async function loadRole(){if(profile)return profile;const {data:{user}}=await supabaseClient.auth.getUser();if(!user)return null;const {data}=await supabaseClient.from('customers').select('id,email,is_admin,blocked,admin_role').eq('id',user.id).maybeSingle();profile=data||null;return profile;}
async function enforce(){const p=await loadRole();if(!p?.is_admin||p.blocked){location.replace('admin.html');return}document.documentElement.dataset.adminRole=p.admin_role||'admin';if(p.admin_role==='employee'){document.querySelectorAll('a').forEach(a=>{if(hiddenPaths.some(path=>(a.getAttribute('href')||'').includes(path)))a.hidden=true});const page=location.pathname.split('/').pop()||'';if(SENSITIVE_PAGES.has(page)){alert('직원 계정은 거래처 정보·단가·통계·환경설정에 접근할 수 없습니다.');location.replace('picking.html')}}}
async function requireSecurity(level='password'){const p=await loadRole();if(!p)throw new Error('로그인이 필요합니다.');if(p.admin_role==='employee')throw new Error('직원 계정은 이 작업을 수행할 수 없습니다.');const password=prompt('보안을 위해 현재 비밀번호를 입력하세요.');if(!password)throw new Error('본인 확인이 취소되었습니다.');const {error}=await supabaseClient.auth.signInWithPassword({email:p.email,password});if(error)throw new Error('비밀번호가 맞지 않습니다.');await supabaseClient.rpc('mark_admin_password_verified');if(level==='email'){if(p.admin_role!=='developer_admin')throw new Error('최고 보안 작업은 개발관리자만 가능합니다.');const sent=await supabaseClient.auth.signInWithOtp({email:p.email,options:{shouldCreateUser:false}});if(sent.error)throw new Error('인증 메일 발송 실패: '+sent.error.message);const token=prompt('메일로 받은 인증번호를 입력하세요.');if(!token)throw new Error('메일 인증이 취소되었습니다.');const verified=await supabaseClient.auth.verifyOtp({email:p.email,token,type:'email'});if(verified.error)throw new Error('메일 인증번호가 맞지 않습니다.');await supabaseClient.rpc('mark_admin_email_verified');}return true;}
window.requireAdminSecurity=requireSecurity;document.addEventListener('DOMContentLoaded',enforce);
})();
