(()=>{'use strict';if(window.__adminAiInquiryAlertLoaded)return;window.__adminAiInquiryAlertLoaded=true;const client=(typeof supabaseClient!=='undefined'?supabaseClient:window.supabaseClient);if(!client)return;let latestSeen=Number(sessionStorage.getItem('designjam_ai_inquiry_popup_seen')||0),timer=null;const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function close(){document.getElementById('adminAiInquiryPopup')?.remove()}function show(rows){if(!rows.length||document.getElementById('adminAiInquiryPopup'))return;latestSeen=Math.max(latestSeen,...rows.map(x=>Number(x.id||0)));sessionStorage.setItem('designjam_ai_inquiry_popup_seen',String(latestSeen));document.body.insertAdjacentHTML('beforeend',`<div id="adminAiInquiryPopup" class="admin-ai-inquiry-popup"><section><header><div><strong>💬 새 거래처 문의 ${rows.length}건</strong><small>자동답변으로 처리되지 않은 문의입니다.</small></div><button type="button" data-close>×</button></header><div>${rows.slice(0,5).map(x=>`<article><b>${esc(x.customer_name||'거래처')}</b><p>${esc(x.question)}</p><small>${new Date(x.created_at).toLocaleString('ko-KR')}</small></article>`).join('')}</div><footer><button type="button" data-close>나중에 확인</button><button type="button" data-open>문의관리 열기</button></footer></section></div>`);const popup=document.getElementById('adminAiInquiryPopup');popup.querySelectorAll('[data-close]').forEach(b=>b.onclick=close);popup.querySelector('[data-open]').onclick=()=>location.href='ai-inquiries.html'}async function check(){const {data,error}=await client.from('customer_ai_inquiries').select('id,customer_name,question,created_at').eq('status','대기').gt('id',latestSeen).order('id',{ascending:true}).limit(20);if(!error&&data?.length)show(data)}async function start(){const {data:{user}}=await client.auth.getUser();if(!user)return;const {data:p}=await client.from('customers').select('is_admin,blocked,admin_role').eq('id',user.id).maybeSingle();if(!p?.is_admin||p.blocked||['manager','employee'].includes(p.admin_role))return;await check();timer=setInterval(check,20000);try{client.channel('admin-ai-inquiry-alert').on('postgres_changes',{event:'INSERT',schema:'public',table:'customer_ai_inquiries'},payload=>{const row=payload.new;if(Number(row.id)>latestSeen&&row.status==='대기')show([row])}).subscribe()}catch(_){}}start();window.addEventListener('beforeunload',()=>timer&&clearInterval(timer));})();
