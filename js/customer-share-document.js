const supabaseUrl="https://dtjhuejmxrjkcxzvilgw.supabase.co",supabaseKey="sb_publishable_kwXvFOCpknkDf9BKmcszrQ_Q7IBVg87";
const supabaseClient=window.supabase.createClient(supabaseUrl,supabaseKey),params=new URLSearchParams(location.search),orderNumber=params.get('order');
const area=document.getElementById('shareDocument'),recipientSelect=document.getElementById('recipientSelect');let customerId=null,profiles=[],activeKey='',items=[];
const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const keyFor=(n,a)=>`${String(n||'').trim().toLowerCase().replace(/\s+/g,'')}|${String(a||'').trim().toLowerCase().replace(/\s+/g,'')}`;
const field=n=>area.querySelector(`[data-field="${n}"]`)?.textContent?.trim()||'';
function editAll(){area.querySelectorAll('[data-field]').forEach(n=>{const locked=/^(?:total_qty|product_amount|total_amount|row_\d+_amount|row_\d+_status)$/.test(n.dataset.field||'');n.contentEditable=locked?'false':'true';n.spellcheck=false;n.classList.toggle('calculated-field',locked)})}
const editableNumber=value=>Number(String(value||'').replace(/[^0-9.-]/g,''))||0;
function soldoutInfo(item){const ordered=Math.max(0,Number(item.qty||0)),soldout=Math.min(ordered,Math.max(0,Number(item.soldout_qty||(item.is_soldout?ordered:0))));return{ordered,soldout,shipped:Math.max(0,ordered-soldout),label:soldout>=ordered&&ordered>0?'전체품절':soldout>0?`일부품절 ${soldout}죽`:''}}
function recalculateShareAmounts(){let totalQty=0,productAmount=0;items.forEach((_,index)=>{const qty=Math.max(0,editableNumber(field(`row_${index}_qty`))),price=Math.max(0,editableNumber(field(`row_${index}_price`))),amount=qty*price;totalQty+=qty;productAmount+=amount;const amountCell=area.querySelector(`[data-field="row_${index}_amount"]`);if(amountCell)amountCell.textContent=`${amount.toLocaleString()}원`});const shippingFee=Math.max(0,editableNumber(field('shipping_fee'))),qtyCell=area.querySelector('[data-field="total_qty"]'),productCell=area.querySelector('[data-field="product_amount"]'),shippingCell=area.querySelector('[data-field="shipping_fee"]'),amountCell=area.querySelector('[data-field="total_amount"]');if(qtyCell)qtyCell.textContent=`${totalQty.toLocaleString()}죽`;if(productCell)productCell.textContent=`${productAmount.toLocaleString()}원`;if(shippingCell&&document.activeElement!==shippingCell)shippingCell.textContent=`${shippingFee.toLocaleString()}원`;if(amountCell)amountCell.textContent=`${(productAmount+shippingFee).toLocaleString()}원`}
area.addEventListener('input',event=>{const key=event.target?.dataset?.field||'';if(/^row_\d+_(?:qty|price)$/.test(key)||key==='shipping_fee')recalculateShareAmounts()});
area.addEventListener('blur',event=>{const key=event.target?.dataset?.field||'';if(!(/^row_\d+_(?:qty|price)$/.test(key)||key==='shipping_fee'))return;const value=Math.max(0,editableNumber(event.target.textContent));event.target.textContent=key.endsWith('_qty')?`${value.toLocaleString()}죽`:`${value.toLocaleString()}원`;recalculateShareAmounts()},true);
function displayItemNumber(value){const clean=String(value||'').trim().replace(/^[SBI](?:[-_\s]+|(?=\d))/i,'');if(/A$/i.test(clean))return`${clean.slice(0,-1)} 아동`;if(/M$/i.test(clean))return`${clean.slice(0,-1)} 무지`;return clean}
function render(){const f=items[0],rows=items.map(x=>({...x,...soldoutInfo(x)})),productAmount=rows.reduce((s,x)=>s+x.shipped*Number(x.price||0),0),qty=rows.reduce((s,x)=>s+x.shipped,0),savedShippingRow=items.find(x=>x.shipping_fee_manual===true),shippingFee=savedShippingRow?Math.max(0,Number(savedShippingRow.shipping_fee||0)):4000;area.innerHTML=`<header class="share-head"><div><h1 data-field="title">거래 내역서</h1><p class="brand" data-field="brand">${esc(f.customer_name||'상호명')}</p></div><div data-field="date">${new Date(f.created_at).toLocaleDateString('ko-KR')}</div></header><section class="share-meta"><div><strong data-field="label_recipient">받는 곳</strong><span data-field="recipient">${esc(f.delivery_name||'')}</span></div><div><strong data-field="label_owner">담당자</strong><span data-field="owner"></span></div><div><strong data-field="label_phone">연락처</strong><span data-field="phone">${esc(f.delivery_phone||'')}</span></div><div><strong data-field="label_address">주소</strong><span data-field="address">${esc(f.delivery_address||'')}</span></div><div><strong data-field="label_order">주문번호</strong><span data-field="order_number">${esc(f.order_number)}</span></div><div><strong data-field="label_memo">메모</strong><span data-field="memo">${esc(f.memo||'')}</span></div></section><p class="share-calc-note">품절·일부품절 수량은 자동 차감됩니다. 수량·단가·배송비를 수정하면 최종금액이 자동 계산됩니다.</p><table class="share-table"><thead><tr><th data-field="head_no">번호</th><th data-field="head_item">품번</th><th data-field="head_status">출고상태</th><th data-field="head_qty">출고수량</th><th data-field="head_price">단가</th><th data-field="head_amount">금액</th></tr></thead><tbody>${rows.map((x,i)=>`<tr class="${x.label?'share-soldout-row':''}"><td data-field="row_${i}_no">${i+1}</td><td data-field="row_${i}_item">${esc(displayItemNumber(x.item_number))}</td><td data-field="row_${i}_status">${x.label?`<span class="share-soldout-badge">${esc(x.label)}</span>`:'정상출고'}</td><td data-field="row_${i}_qty">${x.shipped}죽</td><td data-field="row_${i}_price">${Number(x.price||0).toLocaleString()}원</td><td data-field="row_${i}_amount">${(x.shipped*Number(x.price||0)).toLocaleString()}원</td></tr>`).join('')}</tbody></table><section class="share-total"><p><span data-field="label_total_qty">총 출고수량</span><strong data-field="total_qty">${qty}죽</strong></p><p><span data-field="label_product_amount">상품금액</span><strong data-field="product_amount">${productAmount.toLocaleString()}원</strong></p><p><span data-field="label_shipping_fee">배송비</span><strong data-field="shipping_fee">${shippingFee.toLocaleString()}원</strong></p><p class="share-grand-total"><span data-field="label_total_amount">최종금액</span><strong data-field="total_amount">${(productAmount+shippingFee).toLocaleString()}원</strong></p></section><footer class="share-footer"><p data-field="footer_text">위 내용과 같이 거래하였음을 확인합니다.</p><h2 data-field="footer_brand">${esc(f.customer_name||'')}</h2></footer>`;editAll();recalculateShareAmounts()}
function populate(){recipientSelect.innerHTML='<option value="">저장 납품처 선택</option>'+profiles.map(p=>`<option value="${esc(p.recipient_key)}">${esc(p.recipient_name||'이름 없음')}</option>`).join('');recipientSelect.value=activeKey}
function applyProfile(p){if(!p)return;activeKey=p.recipient_key;for(const [k,v] of Object.entries(p.document_json||{})){if(/^(?:total_qty|product_amount|total_amount|shipping_fee|row_\d+_(?:qty|status|amount))$/.test(k))continue;const n=area.querySelector(`[data-field="${CSS.escape(k)}"]`);if(n)n.textContent=v??''}recalculateShareAmounts();populate()}
async function start(){const {data:{user}}=await supabaseClient.auth.getUser();if(!user)return location.replace('login.html');const {data:c}=await supabaseClient.from('customers').select('customer_grade,is_admin').eq('id',user.id).single();if(c?.is_admin||!['우수','우수고객','VIP','VVIP'].includes(String(c?.customer_grade||'')))return location.replace('order.html');customerId=user.id;const r=await supabaseClient.from('orders').select('*').eq('order_number',orderNumber).eq('customer_id',user.id).order('id');if(r.error||!r.data?.length){area.innerHTML='<h2>주문을 찾을 수 없습니다.</h2>';document.body.classList.add('auth-ready');return}items=r.data;render();const pr=await supabaseClient.from('customer_share_document_profiles').select('*').eq('customer_id',user.id).order('updated_at',{ascending:false});profiles=pr.data||[];const k=keyFor(items[0].delivery_name,items[0].delivery_address),found=profiles.find(p=>p.recipient_key===k);if(found)applyProfile(found);populate();document.body.classList.add('auth-ready')}
recipientSelect.onchange=()=>applyProfile(profiles.find(p=>p.recipient_key===recipientSelect.value));
document.getElementById('newRecipientBtn').onclick=()=>{activeKey='';['recipient','owner','phone','address'].forEach(k=>{const n=area.querySelector(`[data-field="${k}"]`);if(n)n.textContent='' });recipientSelect.value=''};
document.getElementById('saveBtn').onclick=async()=>{const recipient=field('recipient'),address=field('address');if(!recipient)return alert('납품처명을 입력하세요.');activeKey=keyFor(recipient,address);const json={};area.querySelectorAll('[data-field]').forEach(n=>json[n.dataset.field]=n.textContent.trim());const payload={customer_id:customerId,recipient_key:activeKey,recipient_name:recipient,owner_name:field('owner'),phone:field('phone'),delivery_address:address,document_json:json,updated_at:new Date().toISOString()};const {error}=await supabaseClient.from('customer_share_document_profiles').upsert(payload,{onConflict:'customer_id,recipient_key'});if(error)return alert('저장 실패: '+error.message+'\nSQL/V6.5.56-CUSTOMER-SHARE-DOCUMENT.sql을 실행하세요.');const i=profiles.findIndex(p=>p.recipient_key===activeKey);if(i>=0)profiles[i]=payload;else profiles.unshift(payload);populate();alert('문서와 납품처 정보가 저장되었습니다.')};
document.getElementById('deleteBtn').onclick=async()=>{if(!activeKey||!confirm('선택한 납품처와 저장 문서 양식을 삭제할까요?'))return;const {error}=await supabaseClient.from('customer_share_document_profiles').delete().eq('customer_id',customerId).eq('recipient_key',activeKey);if(error)return alert('삭제 실패: '+error.message);profiles=profiles.filter(p=>p.recipient_key!==activeKey);activeKey='';populate();alert('삭제했습니다.')};
const safeFileName=value=>String(value||'거래처').replace(/[\\/:*?"<>|]/g,'_').trim()||'거래처';
async function saveHighResolutionDocument(){
  const clone=area.cloneNode(true);clone.removeAttribute('id');clone.classList.add('share-capture-master');
  clone.style.cssText='position:fixed;left:-10000px;top:0;width:1000px;max-width:none;margin:0;padding:34px;box-shadow:none;background:#fff;color:#10243a;z-index:-1';
  clone.querySelectorAll('[contenteditable]').forEach(node=>node.contentEditable='false');document.body.appendChild(clone);
  try{
    await document.fonts?.ready;await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve)));
    const width=Math.max(1000,clone.scrollWidth),height=Math.max(1,clone.scrollHeight),scale=Math.max(.1,Math.min(3,30000/height,12000/width));
    const canvas=await html2canvas(clone,{scale,backgroundColor:'#fff',useCORS:true,logging:false,windowWidth:width,windowHeight:height,scrollX:0,scrollY:0});
    const blob=await new Promise((resolve,reject)=>canvas.toBlob(value=>value?resolve(value):reject(new Error('이미지 변환 실패')),'image/png'));
    const a=document.createElement('a');a.download=`${safeFileName(field('recipient'))}_${new Date().toISOString().slice(0,10)}_전달문서.png`;a.href=URL.createObjectURL(blob);a.click();setTimeout(()=>URL.revokeObjectURL(a.href),5000);
  }finally{clone.remove()}
}
document.getElementById('captureBtn').onclick=async function(){if(!window.html2canvas)return alert('이미지 기능을 불러오지 못했습니다.');const old=this.textContent;this.disabled=true;this.textContent='고해상도 저장 중';try{await saveHighResolutionDocument()}catch(error){console.error(error);alert('고해상도 이미지 저장에 실패했습니다. 브라우저를 새로고침한 뒤 다시 시도해주세요.')}finally{this.disabled=false;this.textContent=old}};start();
