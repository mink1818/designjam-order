DESIGN SOCKS V6.5.1 배포 방법

1. Supabase SQL Editor에서 아래 파일을 전체 실행합니다.
SQL/V6.5.1-CUSTOMER-PRICE-IMPORT-FIX.sql

2. 관리자 상품관리에서 첨부 엑셀을 다시 선택하고 실제 반영합니다.
완료 결과의 "거래처별 단가" 저장 건수가 0보다 큰지 확인합니다.
이름이 일치하지 않는 거래처는 결과 화면에 별도로 표시됩니다.

3. 터미널에서 배포합니다.
git add .
git commit -m "release v6.5.1 customer price import and proxy quantity parser"
git push

4. 배포 후 브라우저에서 Ctrl+F5로 새로고침합니다.

SQL 실행: 필요
Supabase Functions 재배포: 불필요
Git Push: 필요
